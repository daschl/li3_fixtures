{
	"category": "libraries",
	"title":    "li3_fixtures",
	"description": "The `li3_fixtures` library adds the ability to separate your test data from your test code using fixtures pulled from different data sources (currently json and php files)."
}
